# Disabled — this is a Vite/React project, not a Deno project.
# The default Deno workflow was added by mistake.
# CI runs via Lovable's built-in checks instead.

name: Deno (disabled)

on:
  workflow_dispatch: # manual trigger only

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use Lovable CI or add a Node-based workflow."
